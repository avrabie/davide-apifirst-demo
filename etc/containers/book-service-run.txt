docker run -d
    --name polar-postgres
    -e POSTGRES_USER=user
    -e POSTGRES_PASSWORD=password
    -e POSTGRES_DB=polardb_catalog
    -p 5432:5432
    postgres


docker start polar-postgres
docker stop polar-postgres
docker rm -fv polar-postgres


 docker run -d --name polar-postgres -e POSTGRES_USER=user -e POSTGRES_PASSWORD=password -e POSTGRES_DB=polardb_catalog -p 5432:5432 postgres

 //dump db
 docker exec -t polar-postgres pg_dumpall -c -U user > dump_iaka.sql